# Offline research notebooks

Notebooks are for research only (TDA, Matrix Profile, ablation exploration).
Production decisions must be reproducible by code under `src/` and must not
depend on notebook state.
