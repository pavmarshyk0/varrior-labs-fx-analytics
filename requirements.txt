# Runtime conveniences for a full local research workstation.
# Core package dependency metadata lives in pyproject.toml.
pyarrow>=16
pandas>=2.2
plotly>=5.24
streamlit>=1.36
MetaTrader5>=5.0; platform_system == "Windows"
